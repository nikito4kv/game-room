# three@0.185 конфликтует с peer-зависимостью postprocessing (three < 0.185).
# Конфликт предсуществующий; ставим с legacy-peer-deps, чтобы npm install
# (локально и на Vercel/CI) не падал с ERESOLVE.
legacy-peer-deps=true
